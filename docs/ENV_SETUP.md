# Environment Setup

1. Review `DEPLOYMENT.md` for required vs optional environment variables.
2. Copy `.env.example` values into your local environment as needed.
3. Configure the same variables in Netlify Site Settings for production.
4. Run `npm install` followed by `npm run dev` for local development.
