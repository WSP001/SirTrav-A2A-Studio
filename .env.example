# Required (production)
URL=https://your-site.netlify.app
PUBLISH_TOKEN_SECRET=replace-with-strong-token

# Optional auth gates
MCP_SECRET_TOKEN=replace-if-using-mcp

# Storage (only netlify_lm is supported in this repo)
STORAGE_BACKEND=netlify_lm

# Optional integrations (stubbed in this public scaffold)
ELEVENLABS_API_KEY=
ELEVENLABS_DEFAULT_VOICE_ID=
SUNO_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
